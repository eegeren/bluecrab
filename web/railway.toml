[build]
builder = "NIXPACKS"

[build.nixpacks]
providers = ["nodejs-pnpm", "nextjs"]

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
